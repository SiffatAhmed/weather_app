import 'package:flutter/material.dart';

const windowPadding = EdgeInsets.symmetric(vertical: 40, horizontal: 16);
const cardPadding = EdgeInsets.symmetric(vertical: 16, horizontal: 8);

//colors
const primaryBlue = Color.fromRGBO(33, 68, 243, 1);
const textBlackColor = Color(0xFF8D8E98);

//Weather walay default coordinates
var defaultPosition = {
  "longitude": 73.0509519,
  "latitude": 33.6586571,
};
